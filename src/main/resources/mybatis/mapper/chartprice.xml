<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fsd2020.data.mapper.PriceMapper">

	<!--  -->
	<insert id="insertPrice">
		insert into 
			stock_price_detail (
					company_code, 
					stock_exchange, 
					price,
					price_min) 
			VALUES 
			<foreach collection="prices" item="price" separator=",">
				(
					#{price.code}, 
					#{price.stockExchange}, 
					#{price.price},
					#{price.date} )
			</foreach>
	</insert>
	
	
</mapper>